{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@h-gpt/types": "workspace:*",
    "@h-gpt/workflow-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
